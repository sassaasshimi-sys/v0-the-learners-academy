// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Teacher {
  id               String       @id @default(cuid())
  name             String
  email            String       @unique
  phone            String
  employeeId       String       @unique
  subjects         String[]
  qualifications   String[]
  status           String       @default("active")
  avatar           String?
  joinedAt         DateTime     @default(now())
  coursesCount     Int          @default(0)
  studentsCount    Int          @default(0)
  assignedClass    String?
  employeePassword String?
  courses          Course[]
  assignments      Assignment[]
  attendance       TeacherAttendance[]
}

model TeacherAttendance {
  id           String   @id @default(cuid())
  teacherId    String
  teacher      Teacher  @relation(fields: [teacherId], references: [id])
  date         DateTime @default(now())
  status       String   // "Present", "Absent", "Late", "Leave"
  isSubstitute Boolean  @default(false)
  note         String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@unique([teacherId, date])
}

model Admin {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  name      String
  role      String   @default("admin")
  createdAt DateTime @default(now())
}

model Student {
  id              String       @id @default(cuid())
  name            String
  email           String       @unique
  phone           String
  password        String?      // Added for DB auth
  enrolledCourses String[]
  status          String       @default("active")
  avatar          String?
  guardianName    String?
  studentId       String?      @unique
  enrolledAt      DateTime     @default(now())
  progress        Float        @default(0)
  grade           String?
  classTiming     String?
  submissions     Submission[]
  feePayments     FeePayment[]
}

model Course {
  id          String       @id @default(cuid())
  title       String
  description String
  level       String
  teacherId   String
  teacherName String
  teacher     Teacher      @relation(fields: [teacherId], references: [id])
  capacity    Int
  enrolled    Int          @default(0)
  status      String       @default("active")
  schedule    String
  duration    String
  startDate   DateTime
  endDate     DateTime
  roomNumber  String?
  thumbnail   String?
  feeAmount   Float        @default(0)
  assignments Assignment[]
  feePayments FeePayment[]
}

model FeePayment {
  id           String    @id @default(cuid())
  studentId    String
  courseId     String
  amountPaid   Float     @default(0)
  totalAmount  Float     @default(0)
  status       String    @default("Unpaid") // "Paid", "Partial", "Unpaid"
  paymentDate  DateTime?
  updatedAt    DateTime  @updatedAt
  createdAt    DateTime  @updatedAt
  student      Student   @relation(fields: [studentId], references: [id])
  course       Course    @relation(fields: [courseId], references: [id])

  @@unique([studentId, courseId])
}

model Expenditure {
  id          String   @id @default(cuid())
  amount      Float
  category    String   // "Salaries", "Infrastructure", "Supplies", "Marketing", "Other"
  description String
  date        DateTime @default(now())
  createdAt   DateTime @default(now())
}

model Assignment {
  id               String       @id @default(cuid())
  title            String
  description      String
  courseId         String
  courseName       String
  course           Course       @relation(fields: [courseId], references: [id])
  teacherId        String
  teacher          Teacher      @relation(fields: [teacherId], references: [id])
  dueDate          DateTime
  status           String       @default("active")
  submissionsCount Int          @default(0)
  totalStudents    Int          @default(0)
  createdAt        DateTime     @default(now())
  submissions      Submission[]
}

model Submission {
  id               String     @id @default(cuid())
  assignmentId     String
  assignmentTitle  String
  assignment       Assignment @relation(fields: [assignmentId], references: [id])
  studentId        String
  studentName      String
  student          Student    @relation(fields: [studentId], references: [id])
  submittedAt      DateTime   @default(now())
  status           String     @default("pending")
  grade            Float?
  feedback         String?
  fileUrl          String?
  randomizedQuestions Json?
  answers          Json?
  aiFeedback       String?
  aiJustification  String?
}

model Question {
  id            String   @id @default(cuid())
  category      String
  type          String   // "MCQ" | "Subjective" | "True/False" | "Fill in the Blanks" | "Writing" | "Matching"
  content       String
  options       String[]
  correctAnswer String?
  imageUrl      String?
  phase         String   // "First Test" | "Last Test" | "Both"
  passageText   String?  // Reading: full passage text shown above the question
  audioUrl      String?  // Listening: audio clip URL auto-played on question load
  matchPairs    Json?    // Matching: [{left: "term", right: "definition"}, ...]
}

model AssessmentTemplate {
  id              String   @id @default(cuid())
  title           String
  phase           String // "First Test" | "Last Test"
  classLevels     String[]
  nature          String // "MCQ" | "Subjective" | "Mixed"
  totalMarks      Int
  durationMinutes Int
  questionCount   Int      @default(10)
  createdAt       DateTime @default(now())
  status          String   @default("active")
  accessCode      String
}

model Schedule {
  id         String   @id @default(cuid())
  classTitle String
  teacherName String
  timing     String
  roomNumber String
  days       String[]
  slotId     String?
}
